[project]
name = "awesome-object-detection"
version = "0.1.0"
description = "Machine-readable object-detection papers for YOLO-Agent"
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]
